package com.minhaprimeiraapijava;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MinhaApiApplication {

	public static void main(String[] args) { //no java isso que define onde a aplicação vai rodar
		SpringApplication.run(MinhaApiApplication.class, args);
	}
}